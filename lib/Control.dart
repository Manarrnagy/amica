class Control {
  static void addToList(List list, var obj) {
    list.add(obj);
  }

  // static double setRotation(int degree) {
  //   final angle = degree * pi / 180;
  //   animation = Tween<double>(begin: 0, end: angle);
  //   return angle;
  // }
}
